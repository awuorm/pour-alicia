import React from "react";
import { StyledUserDrawer } from "../styles";

const UserDrawer = () => {
  return (<StyledUserDrawer>
            Hello from UserDrawer!
  </StyledUserDrawer>);
};

export default UserDrawer;
